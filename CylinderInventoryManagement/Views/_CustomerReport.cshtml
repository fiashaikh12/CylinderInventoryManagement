@model List<CIM.Entities.CustomerReportResponse>
<div class="box-header">
    <h3 class="box-title">Inventory List</h3>
</div>
<!-- /.box-header -->
<div class="box-body">
    <table id="example1" class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>Ledger Date</th>
                <th>Challan Number</th>
                <th>Gas Type</th>
                <th>FullIssue</th>
                <th>Return</th>
                <th>Holding</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null)
            {
                foreach (var item in Model)
                {
                    <tr>
                        <td>@item.LedgerDate</td>
                        <td>@item.ChallanNumber</td>
                        <td>@item.CategoryName</td>
                        <td>@item.FullIssue</td>
                        <td>@item.ReturnStock</td>
                        <td>@item.HoldingStock</td>
                    </tr>
                }
            }
        </tbody>
    </table>

    @*<table class="hidden" id="tblCustomerReportprint" border="1">
        <thead>
            <tr>
                <th>Ledger Date</th>
                <th>Challan Number</th>
                <th>Gas Type</th>
                <th>FullIssue</th>
                <th>Return</th>
                <th>Holding</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null)
            {
                foreach (var item in Model)
                {
                    <tr>
                        <td>@item.LedgerDate</td>
                        <td>@item.ChallanNumber</td>
                        <td>@item.CategoryName</td>
                        <td>@item.FullIssue</td>
                        <td>@item.ReturnStock</td>
                        <td>@item.HoldingStock</td>
                    </tr>
                }
            }
        </tbody>
    </table>*@
</div>
